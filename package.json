{
  "name": "calendar",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./**/*.js",
    "test": "node ./test/helpers/download-browsers.js && mocha ./test/*.js",
    "build": "rm -rf build/ && cp -r src/ build/"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "firebase": "^3.0.3"
  },
  "devDependencies": {
    "eslint": "^2.13.1",
    "eslint-config-google": "^0.6.0",
    "moment": "^2.13.0",
    "selenium-assistant": "^0.1.5"
  }
}
