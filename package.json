{
  "name": "calendar",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "build": "rm -rf build/ && cp -r src/ build/"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "firebase": "^3.0.3"
  }
}
